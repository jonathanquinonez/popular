<LinearLayout
    android:id="@+id/rootView"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/btn_action_bar">

        <ImageView
            android:id="@+id/sidebarItemImageview"
            android:contentDescription="@string/desc_menu_icon"
            style="@style/SidebarItemImage" />

        <TextView
            android:id="@+id/sidebarTitleTextView"
            style="@style/SidebarMenuEntry"
            android:text="@string/accounts_sidebar" />
    </LinearLayout>

    <LinearLayout style="@style/SidebarMenuSeparator" />

</LinearLayout>